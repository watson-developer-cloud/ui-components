{% macro codeblock() %}

<div class="code-block">
  <div class="code-block--code">
    <pre class="base--pre">
{{ caller() | replace('<', '&lt;') | trim() }}
    </pre>
  </div>
  <div class="code-block--ui">
    {{ caller() }}
  </div>
</div>

{% endmacro%}
